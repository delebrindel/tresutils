export enum PLURALIZE {
  SINGULAR = 'Cheese',
  PLURAL = 'Cheeses',
}

export enum FORMAT {
  NEGATIVE_ENTRY = -12345,
  NEGATIVE_OUTPUT = '-12,345',
  POSITIVE_ENTRY = 7890,
  POSITIVE_OUTPUT = '7,890',
  INVALID_OUTPUT = '-',
}